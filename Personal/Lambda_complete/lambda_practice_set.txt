==============================
LAMBDA FUNCTION PRACTICE QUESTIONS
==============================

1. Create a lambda function to find the square of a number.

2. Create a lambda function to find the greater of two numbers.

3. Create a lambda function to check whether a number is even or odd.

4. Sort a list of strings by their length using sorted() and a lambda function.

5. Sort a list of tuples based on the second element using a lambda function.

6. Use map() and a lambda function to double every number in a list.

7. Use filter() and a lambda function to extract only even numbers from a list.

8. Use map() and a lambda function to find the cube of every number in a list.

9. Sort a list of dictionaries based on the "marks" key using a lambda function.

10. Use filter() and a lambda function to return only the words that start with a vowel.